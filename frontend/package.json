{
  "name": "cu-mortgage-pipeline-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev":     "vite",
    "build":   "vite build",
    "preview": "vite preview --host --port ${PORT:-4173}",
    "start":   "vite preview --host --port ${PORT:-4173}"
  },
  "dependencies": {
    "react":     "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite":                 "^5.3.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
